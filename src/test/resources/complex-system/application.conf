modules = [
  "myModule=test:io.knotx.launcher.VertifiableVerticle"
]

global {
  profile{
    include required(classpath("complex-system/modules/myModule.conf"))
  }
}

config.myModule.options.config {
  myValueKey = ${global.profile.myValueKey}
}