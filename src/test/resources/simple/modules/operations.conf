routingOperations = ${routingOperations} [
  {
    operationId = oid
    handlers = [
      {
      name = name
      config = {include required(classpath("simple/modules/config.conf"))}
      config = {
        variable = ${var}
      }
    }
    ]
  }
]