modules {
  myModule = "test:io.knotx.launcher.VertifiableVerticle"
}

config.myModule.options.config {
  myValueKey = myValue
}

config.myModule.options.config {include required(classpath("simple/modules/myModule.conf"))}